define(['ultra/ultra', 'underscore'], function(Ultra, _) {

	Ultra.Web3DEngine.RenderSystem.RenderTarget = function(target, config) {
		this.target = target;
		this.config = config;

		this.data = {};
	};

	_.extend(Ultra.Web3DEngine.RenderSystem.RenderTarge.prototype, {

	});
});